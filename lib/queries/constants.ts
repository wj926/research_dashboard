// Phase 20: the old hardcoded CURRENT_USER lived here. It has been removed;
// server code should use `getCurrentUserLogin()` from `@/lib/session` instead,
// and client code should use `useSession()` from `next-auth/react`.

export {};
